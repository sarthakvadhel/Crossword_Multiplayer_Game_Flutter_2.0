name: crossword_master
version: 0.1.0
publish_to: 'none'

description: Crossword multiplayer style word game.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  google_sign_in: ^6.2.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true
