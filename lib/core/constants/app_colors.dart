import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const background = Color(0xFFF3F6FB);

  // Primary & Secondary
  static const primary = Color(0xFF2F80ED);
  static const secondary = Color(0xFFBBD7FF);

  // Card Gradients
  static const dailyPuzzleGradientStart = Color(0xFF3B9FFF);
  static const dailyPuzzleGradientEnd = Color(0xFF1E7FCC);

  static const tournamentGradientStart = Color(0xFFFFA500);
  static const tournamentGradientEnd = Color(0xFFFF8C00);

  // Game Board
  static const boardBorder = Color(0xFF94A3B8);
  static const boardTile = Colors.white;
  static const clueTile = Color(0xFFDCEBFF);

  // Text
  static const textDark = Color(0xFF1F2937);
  static const textDarkNavy = Color(0xFF1A2F5A);
  static const textMuted = Color(0xFF6B7280);
  static const textLight = Colors.white;

  // Accent
  static const accent = Color(0xFFFFC466);
  static const successSoft = Color(0xFFDCFCE7);
  static const warningSoft = Color(0xFFFFF4E5);
  static const successText = Color(0xFF166534);
  static const warningText = Color(0xFF9A3412);
  static const flashHighlight = Color(0xFF86EFAC);
  static const danger = Color(0xFFEF4444);

  // Card Button Colors
  static const dailyPlayButton = Color(0xFF6BBFFF);
  static const tournamentPlayButton = Color(0xFFFFB366);
}
